<table>
  <tr><th> Artist</th>
    <th> Album Title </th>
    <th> Genre </th>
    <th> Quantity </th>
    <th> Stock Level </th>
  </tr>
<% @album.each do |album| %>
<tr>
  <td> <%= Artist.find(album.artist_id).name %></td>
  <td> <%= album.title %></td>
  <td> <%= album.genre %></td>
  <td> <%= album.quantity %></td>
  <td> <%= album.stock_checker %></td>
</tr>

<% end %>



</table>
